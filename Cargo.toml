[package]
name = "deepcode"
version = "0.1.0"
edition = "2021"
description = "A DeepSeek-powered read-only code analysis CLI"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }

